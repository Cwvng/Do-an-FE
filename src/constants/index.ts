export enum Status {
  NEW = 'new',
  IN_PROGRESS = 'in progress',
  DONE = 'done',
  WAITING_REVIEW = 'waiting_review',
  TESTING = 'testing',
  FEEDBACK = 'feedback',
}
export enum Priority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  URGENT = 'urgent',
}
